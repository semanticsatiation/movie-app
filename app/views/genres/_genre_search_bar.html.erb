<%= form_tag(movies_genre_path, :method => "get", id: "genres_search-form") do %>
  <%= text_field_tag :search, params[:search], placeholder: "Search #{@genre.category} movies" %>
  <%= submit_tag "Search" %>
<% end %>
